# ───────────── PATH ───────────── 
alias lg='lazygit'

# ───────────── Eza (better ls) ─────────────
alias ls="eza --icons=always"
alias ll="eza -la --icons=always"

# ──────────── Zoxide (better cd) ─────────────
alias cd="z"

# ──────────── Clear Command ─────────────
alias cl='clear'

# ─────────── Quick Exit ─────────────
alias q='exit'

# ─────────── Tmux Shortcuts ─────────────
alias ta="tmux attach"

# ─────────── fastfetch Shortcuts ─────────────
alias ff="fastfetch"

# ─────────── Git Shortcuts ─────────────
alias g='git'

# 
alias lzg='lazygit'
alias lzd='lazydocker'
